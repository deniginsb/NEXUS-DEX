{
  "installCommand": "yarn install",
  "buildCommand": "npm run build",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1",
      "headers": {
        "x-vercel-protection-bypass": "true"
      }
    }
  ]
}
